{
  "name": "solana-wallet-tracker",
  "version": "1.0.0",
  "description": "Tracks Solana wallet activity and sends Telegram alerts",
  "main": "tracker.js",
  "scripts": {
    "start": "node tracker.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@solana/web3.js": "^1.73.0",
    "axios": "^1.6.7",
    "axios-rate-limit": "^3.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
